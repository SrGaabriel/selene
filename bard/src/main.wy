func callback(a: lambda(int32) -> int32) {
    result := a(42);
    println(result);
}

func main() {
    println("Hello, World!");
    callback(lambda|f: int32| f + 12);
    callback(lambda|s: int32| s + 24);
    callback(lambda|t: int32| t + 36);
}