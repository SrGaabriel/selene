func callback(a: lambda(int32) -> int32) {
    result := a(42);
    println(result);
}

func main() {
    println("Hello, World!");
    callback(lambda|x: int32| x + 12);
    callback(lambda|x: int32| x + 24);
    callback(lambda|x: int32| x + 36);
}