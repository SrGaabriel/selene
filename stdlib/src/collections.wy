trait Iterable(
    next() :: string
)

data Collection(
    array: string[5],
    cursor: int32
)

make Collection into Iterable {
    func next(mut self) :: string {
        //self.cursor = self.cursor + 1;
        //array := self.array;
        //cursor := self.cursor;
        //element := array[cursor];
        //return element;
        return "hello";
    }
}